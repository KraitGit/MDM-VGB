defaults:
  - protein_scaffold_inference.yaml

output: outputs/protein_scaffold/base_rollout.pt
seed: 0

generation:
  state_rollout: true

rollout:
  num_rollouts: 10
  snapshots_per_rollout: 3
  val_frac: 0.1
