defaults:
  - dyck_inference.yaml

training:
  data: outputs/dyck/base_rollout.pt
  output_dir: outputs/dyck/verifier
  verifier:
    vocab_size: 8
    mask_id: 6
    max_length: 64
    hidden_dim: 256
    num_layers: 4
    num_heads: 4
    dropout: 0.25
    epochs: 10
    global_batch_size: 256
